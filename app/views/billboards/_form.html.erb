<h1><%= @billboard.id ? 'Edit a Billboard' : 'Create a Billboard' %></h1>

<%= form_for @billboard do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true %>

  <%= f.submit %>
<% end %>